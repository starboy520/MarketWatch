# 依赖声明在 setup.py 的 install_requires。
# setuptools：供 pip install -e . 构建，并让 IDE/Pylance 能解析 setup.py 中的 import。
# 推荐：先建 venv，再执行：
#   pip install -r requirements.txt
#
setuptools>=61
requests>=2.28
# 可编辑安装本地包（会安装 setup.py 中的依赖）：
-e .
#
# 编排流水线（LangGraph + LangChain 1.x + DeepSeek 官方集成）：
#   pip install -e ".[pipeline]"
