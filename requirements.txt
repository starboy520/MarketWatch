# 在项目根目录：python -m venv .venv && source .venv/bin/activate && pip install -U pip
# pip install -r requirements.txt
#
# 本文件对齐 setup.py：基础依赖 + 可编辑安装 news-agent + pipeline 编排栈（LangGraph / LangChain / DeepSeek）。

setuptools>=61

# 与 setup.py install_requires 一致（pip install -e 时也会装上）
requests>=2.28
tomli>=2.0.1; python_version < "3.11"

# 本地包 + extras [pipeline]（langgraph、langchain、langchain-deepseek 等）
-e ".[pipeline]"
